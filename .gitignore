# Build output
/dist/
/bin/
*.out
*.test
coverage.*

# Runtime artifacts — the default mirror root and the push lock
/.m-cache/
.irissync-push.lock

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Claude Code — personal/machine-local permissions (keep settings.json committed)
.claude/settings.local.json
